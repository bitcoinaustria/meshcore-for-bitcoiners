% MeshCore for Bitcoiners — a Bitcoin Austria talk.
% Build from the repo ROOT:  make           (or: latexmk -xelatex meshcore-for-bitcoiners.tex)
% Requires the `theme/` submodule (Bitcoin Austria beamer style). If it is
% missing, run:  git submodule update --init --recursive
%
% NOTE: this is a working draft built from handoff-20260626.md; develop further.
\documentclass[aspectratio=169,t,9pt]{beamer}

\usepackage{theme/bitcoin-austria}

% Talk-specific helper: a Bitcoin vs MeshCore comparison with a "where the
% analogy breaks" callout (wraps the theme's generic \comparisonslide).
% Pass an empty 4th argument {} to omit the callout.
\newcommand{\bridgeslide}[4]{%
  \comparisonslide{#1}{Bitcoin}{#2}{MeshCore}{#3}{Where it breaks}{#4}}

% Talk-specific: app / website screenshots, to break up the theory with a look
% at the real thing. Phone screenshots are tall (portrait), so each is shown as
% a "card" — the image with a short caption beneath. Cards are narrow, so two
% (or more) sit side by side on one slide; add more as they arrive.
%   \screenshotcard[width]{image}{caption}
\newcommand{\screenshotcard}[3][0.30\paperwidth]{%
  \begin{minipage}[t]{#1}%
    \centering
    \includegraphics[height=0.50\paperheight,keepaspectratio]{#2}\par
    \vskip0.6em
    {\footnotesize #3\par}%
  \end{minipage}%
}

% \cornerimage is now provided by the theme (bitcoin-austria.sty) — it floats a
% small image into a slide corner over everything, with a default top clearance
% that sits below the headline/page-number band. Call it right before a slide.

\title{MeshCore for Bitcoiners}
\subtitle{Permissionless communication — from money to radio}
\author{Harald Schilly — Bitcoin Austria}
\date{\today}

\begin{document}

\begin{frame}[plain,noframenumbering]
  \titlepage
\end{frame}

% ============================================================ what is MeshCore?
% Bitcoiners don't know MeshCore — introduce it before drawing the parallels.
\fillerslide[A radio mesh you can join without asking]{What is MeshCore?}

% Hook slide: open with the one-line mental model + why a Bitcoiner should care,
% BEFORE the mechanics. Audience are technical Bitcoiners new to MeshCore.
\begin{frame}{The one-line version}
  \begin{itemize}
    \item \textbf{It's Bitcoin's move, applied to talking:} a self-issued
      identity instead of an account, peers relaying instead of infrastructure
    \item Generate a \textbf{keypair} and you're on --- no carrier, no number,
      no SIM, nobody to ask for permission
    \item Cheap radios relay each other in a \textbf{mesh}; coverage grows because
      people join, not because a company builds towers
  \end{itemize}
  \vfill
  \begin{alertblock}{Why a Bitcoiner should care}
    Spectrum is licensed, telcos are KYC gatekeepers, and the network itself is
    the chokepoint. MeshCore routes around all three --- censorship-resistant,
    self-sovereign comms that still work when the grid is down.
  \end{alertblock}
\end{frame}

\begin{frame}{Messaging without the network}
  \begin{itemize}
    \item Off-grid text messaging over long-range \textbf{LoRa} radio —
      no internet, no cell tower, no SIM
    \item Open-source firmware running on cheap LoRa boards
    \item Uses \textbf{licence-free} ISM radio bands — worldwide, region by
      region (EU 868\,MHz; US/AU 915\,MHz; \dots)
  \end{itemize}
  \vfill
  \begin{alertblock}{The EU angle}
    On these bands \emph{anyone} may transmit — no licence, no registration, no
    fee. You only respect the power and duty-cycle limits.
  \end{alertblock}
\end{frame}

% Show the real thing early: a packet that actually routed across Austria, so
% the audience sees it works before we get into the how.
\fullbleedslide[A packet routed across Austria \;\textbullet\; logger-at.meshcore.observer]{observer-package-across-austria-20260626}

% The Bitcoiner's reflex after seeing it work: "so what's the catch?" Name the
% range/bandwidth trade-off now; the throughput bridge later pays it off.
\begin{frame}{The catch --- kilometres of range, bytes of bandwidth}
  \begin{itemize}
    \item \textbf{Long range, tiny pipe:} a single packet can carry \emph{tens}
      of kilometres line-of-sight --- but at roughly the speed of a 1980s modem.
      One LoRa frame is at most \textbf{255 bytes}
    \item \textbf{Store-and-forward, not a phone call:} messages hop repeater to
      repeater with seconds of latency; you text, you don't stream
    \item \textbf{Physics, not policy:} the range buys reach by spending
      bandwidth and time --- and it sips power, so a node runs for days off a
      small battery or a solar panel
  \end{itemize}
  \vfill
  \begin{alertblock}{The right mental model}
    Not WhatsApp-over-radio. Think \textbf{a permissionless telegraph} --- short
    text, long reach, no carrier. The narrow pipe is the price of needing nothing
    but a cheap radio and line of sight. (We come back to \emph{why} that
    constraint is a feature.)
  \end{alertblock}
\end{frame}

\begin{frame}{Where it comes from}
  \begin{itemize}
    \item Grew out of the \textbf{Meshtastic} world — then rebuilt from scratch
    \item Created by \textbf{Scott Powell} in \textbf{late 2024};
      web/mobile clients by Liam Cottle
    \item Andy Kirby — early tester and promoter (YouTube)
    \item A separate network — \emph{not} interoperable with Meshtastic
    \item Project home: \textbf{meshcore.io}
  \end{itemize}
\end{frame}

\begin{frame}{The hardware — tens of euros, not hundreds}
  \begin{itemize}
    \item \textbf{Companion} (your device: screen + Bluetooth) — SenseCAP
      \textbf{T1000-E}, credit-card size with GPS, $\sim$\,40\,EUR
    \item \textbf{Portable with a display} — Heltec LoRa\,32 V3 (OLED,
      $\sim$\,40\,EUR), Seeed Wio Tracker L1 Pro (screen, GPS, case)
    \item \textbf{Repeater} — SenseCAP Solar Node \textbf{P1-Pro}: 5\,W solar +
      battery, runs for days off-grid, pole-mounted; antenna + device
      $\sim$\,130\,EUR
    \item \textbf{Build your own} — Heltec / RAK WisBlock boards
  \end{itemize}
\end{frame}

% Practical example, right after "the hardware is cheap": the four steps to
% actually get on the mesh that night — buy, pair, generate, join.
\begin{frame}{Your first five minutes on the mesh}
  \begin{itemize}
    \item \textbf{Get a companion board} --- e.g.\ a SenseCAP \textbf{T1000-E}
      ($\sim$40\,EUR), often pre-flashed; otherwise flash it from the browser
    \item \textbf{Install the app} --- Liam Cottle's MeshCore client
      (iOS / Android / web) and pair the board over \textbf{Bluetooth}
    \item \textbf{It generates your keypair on first run} --- that pair \emph{is}
      your identity. Back it up; nobody else can recover it
    \item \textbf{Join} --- type a channel name and you're talking: no account,
      no SIM, no permission
  \end{itemize}
  \vfill
  \begin{alertblock}{That's the whole onboarding}
    Buy, pair, generate, join --- the permissionless claim is literal: nobody
    issued you anything, and nothing on the network knew you were coming.
  \end{alertblock}
\end{frame}

% MeshCore (red, the subject) vs Meshtastic — generic comparison, not \bridgeslide
\comparisonslide{MeshCore vs Meshtastic}%
  {MeshCore}{%
    \begin{itemize}
      \item Role-based: companion (handset), repeater (always-on
        relay), room server (store-and-forward)
      \item \textbf{Source-routed} along discovered paths
      \item Quieter, more scalable mesh
    \end{itemize}}%
  {Meshtastic}{%
    \begin{itemize}
      \item Easy to join, phone-first
      \item Flood-based — every node rebroadcasts
      \item Gets noisy as it grows
    \end{itemize}}%
  {Same cheap hardware}{%
    Same boards, same chips — a different firmware and routing philosophy.}

% Break up the theory with a look at the real app: watch it, chat, plan a link.
\begin{frame}{Live on the mesh}
  \vfill
  \centering
  \screenshotcard[0.28\paperwidth]{meshcore-observer-live-map-vienna}{%
    The observer's live map: 828 nodes, packets arcing across Vienna in
    real time.}%
  \hspace{1.4em}%
  \screenshotcard[0.28\paperwidth]{meshcore-app-test-channel}{%
    The public \textbf{\#test} channel: signed adverts and \emph{ping}\,/\,%
    \emph{pong} probes scrolling by.}%
  \hspace{1.4em}%
  \screenshotcard[0.28\paperwidth]{meshcore-app-line-of-sight-vienna}{%
    Line-of-sight planner: terrain and Fresnel zone between two repeaters
    (6.7\,km in Vienna).}%
  \vfill
\end{frame}

% The flip side of source routing: a real message and the path it travelled.
% Content is top-aligned (not vfill-centred) so the captions sit directly under
% the images, not pushed down to the slide edge.
\begin{frame}{How a message reaches me}
  \vspace{0.8em}
  \centering
  \begin{minipage}[t]{0.44\paperwidth}%
    \centering
    \includegraphics[width=0.44\paperwidth,keepaspectratio]{meshcore-app-coffee-message}\par
    \vskip0.7em
    {\footnotesize A single message on the public \textbf{\#coffee} channel,
     opened to show its metadata: path-hash size, SNR, and the hop count it
     picked up crossing the mesh.}%
  \end{minipage}%
  \hspace{2.5em}%
  \begin{minipage}[t]{0.30\paperwidth}%
    \centering
    \includegraphics[height=0.46\paperheight,keepaspectratio]{meshcore-app-path-7hops}\par
    \vskip0.7em
    {\footnotesize \dots\ and a delivery path traced hop by hop: each
     \emph{known repeater} that relayed the message, from sender to me.}%
  \end{minipage}%
  \vfill
\end{frame}

% Make the agenda + the honesty contract explicit before the thesis, so the
% later "where it breaks" callouts land as a kept promise, not a hedge.
\begin{frame}{What recurs --- and a promise}
  You already know the moves. The rest of the talk is the same Bitcoin
  primitives, in radio:
  \vskip0.8em
  \begin{itemize}
    \item \textbf{A self-issued keypair} is your whole identity --- no issuer, no
      registration
    \item \textbf{Signatures and encryption} do the work --- adverts signed,
      DMs encrypted
    \item \textbf{A throughput cap you didn't choose} forces a second layer ---
      sound familiar?
    \item \textbf{Routing, not flooding} --- find a path, don't shout to everyone
    \item \textbf{Permissionless by construction} --- you join by generating a
      key, not by asking
  \end{itemize}
  \vfill
  \begin{alertblock}{The honest contract for the rest of this talk}
    Every parallel is drawn rigorously --- and every time it \textbf{breaks},
    I'll say so out loud. MeshCore is not Bitcoin; it's Bitcoin's \emph{instincts}
    pointed at a different problem. The breaks are where it gets interesting.
  \end{alertblock}
\end{frame}

% ---------------------------------------------------------------- core thesis
\begin{frame}{The core thesis}
  We opened with the one-liner; here is the precise claim the rest of the talk
  defends.
  \vskip0.6em
  \begin{itemize}
    \item A self-generated \textbf{keypair} replaces institutional identity.
    \item \textbf{Bitcoin} replaces the bank account — gatekeeper: the bank.
    \item \textbf{MeshCore} replaces the SIM card / call sign — gatekeepers:
      the telco \emph{and} the state.
    \item No registration, no permission: you simply become a full participant.
  \end{itemize}
  \vfill
  \begin{alertblock}{The line the talk hangs on}
    Radio spectrum is heavily licensed. A permissionless, self-issued identity
    broadcasting in the ether is arguably even more subversive than
    permissionless money.
  \end{alertblock}
\end{frame}

% ---------------------------------------------------------------- identity
\fillerslide[The strongest parallel]{Identity is a keypair}

\bridgeslide{Identity is a keypair}{%
  \begin{itemize}
    \item Self-generated keypair, no issuer
    \item Address = hash of the public key
    \item ``Not your keys, not your coins''
    \item Verify the address out-of-band
  \end{itemize}}{%
  \begin{itemize}
    \item Self-generated keypair, on-device
    \item Routing target = public-key prefix
    \item ``Not your keys, not your identity''
    \item Verify keys via QR / word list (TOFU)
  \end{itemize}}{%
  HD wallets rotate addresses for privacy; a MeshCore identity is deliberately
  \emph{sticky} — you want to stay discoverable. No HD equivalent, and no seed
  phrase: lose the device and you lose the \emph{handle} — but with no value at
  stake, a re-announce, not a theft.}

% ---------------------------------------------------------------- signing
\fillerslide[The elegant twist]{The signing model}

\bridgeslide{Opposite jobs for the same keypair}{%
  \begin{itemize}
    \item Content is \textbf{public} (on-chain)
    \item The key protects \textbf{authorization}
    \item No valid signature → the transaction does not exist
  \end{itemize}}{%
  \begin{itemize}
    \item Identity is \textbf{public} (the \textbf{advert} --- a node's signed
      ``here is my key, here I am'')
    \item The key protects \textbf{content}
    \item Adverts are signed (Ed25519); DMs are encrypted (X25519)
  \end{itemize}}{%
  Group / channel messages use a pre-shared key — no per-sender signature.
  Anyone with the PSK can read \emph{and} write. (An honest flaw to name.)}

% ---------------------------------------------------------------- throughput
\fillerslide[A chosen constraint, not a weakness]{Throughput}

% EU 868 duty cycle VERIFIED (CEPT/ERC 70-03 + ETSI EN 300 220-2; implemented in
% Austria via the national frequency plan). MeshCore's EU default 869.525 MHz
% falls in the 869.4--869.65 MHz sub-band ("P"/g3) => 10% duty cycle, up to
% 500 mW (+27 dBm) ERP. (The lower 868.0--868.6 MHz sub-band is only 1% at
% 25 mW — MeshCore deliberately picks the generous one.) Sources: The Things
% Network "Regional limitations of RF use"; rf.guru "Meshtastic, MeshCore,
% 868 MHz and the Ham Radio Trap"; disk91.com 868 MHz regulation overview.
% NOTE: still sanity-check against the current AT frequency plan before stage.
% Throughput numbers (estimation, both sides):
%  Bitcoin: ~2,000-3,000 tx/block, 1 block/10 min => ~3-7 tx/s ~= 200-400 tx/min.
%  MeshCore (AT preset 869.618 MHz, SF8, BW62.5 kHz, CR 4/8): LoRa time-on-air
%  (Semtech ToA: 8-sym preamble, explicit header, CRC, DE=0) for a 16 B short
%  text ~0.25 s, 64 B ~0.64 s, 184 B full packet ~1.62 s; lighter coding 4/5
%  trims ~1/3 (16 B ~0.19 s ... 184 B ~1.05 s). 10% duty cycle = 6 s airtime/min/node
%  => ~24/min (short) down to ~4/min (full packet); ~30/min for tiny pings @4/5.
\bridgeslide{The throttle is imposed, not a failure}{%
  \begin{itemize}
    \item Block size is a \textbf{consensus} cap: one block every $\sim$10\,min
    \item $\sim$2{,}000--3{,}000 tx/block $\Rightarrow$ $\sim$3--7\,tx/s
      $\approx$ \textbf{200--400 tx/min}, network-wide
  \end{itemize}}{%
  \begin{itemize}
    \item Duty cycle is a \textbf{regulatory} cap: \textbf{10\%} airtime on the
      869.4--869.65\,MHz sub-band \,(AT: 869.618\,MHz, SF\,8, BW\,62.5\,kHz, CR\,4/8)
    \item One msg $\approx$ 0.25--1.6\,s on air (short text $\to$ full 184\,B);
      10\% $=$ 6\,s airtime/min $\Rightarrow$ \textbf{$\sim$4--25 msgs/min per node}
  \end{itemize}}{%
  Bitcoin's rate is one \emph{global} ledger; MeshCore's is \emph{per node} on a
  \textbf{shared} channel --- many nodes split the same airtime, so the real
  ceiling is lower. Either way the throttle is imposed, not ``they couldn't do
  more'' --- and a deliberate cap is exactly what forces a second layer to exist.}

% ---------------------------------------------------------------- lightning
\fillerslide[Two distinct analogies — keep them separate]{Lightning Network}

\bridgeslide{Angle A — route along a discovered path}{%
  \begin{itemize}
    \item Find a route through specific channels
    \item Don't broadcast the payment to everyone
  \end{itemize}}{%
  \begin{itemize}
    \item An advert floods once; path info accumulates
    \item Later DMs are source-routed along that path
  \end{itemize}}{%
  Lightning onion-routes (hop privacy) and carries atomic value (HTLCs);
  MeshCore path bytes are in the clear and carry data — no settlement.}

\begin{frame}{Angle B — bootstrap, then jump layers}
  \begin{itemize}
    \item A small mesh message = the rare, expensive anchoring event
    \item Exchange a key / rendezvous over the mesh
    \item Move the bulk transfer to a fat pipe (internet, CB radio) — off-mesh
  \end{itemize}
  \vfill
  \begin{alertblock}{Where it breaks}
    Lightning stays cryptographically chained to L1 (you can unilaterally
    close). The mesh\,→\,fat-pipe handoff is a \textbf{one-time bootstrap}, not
    a trust-anchored channel — closer to WebRTC signaling or a BitTorrent
    tracker.
  \end{alertblock}
\end{frame}

% ---------------------------------------------------------------- facts
\fillerslide[Side by side — what's under the hood]{Crypto facts}

\begin{frame}{The crypto, side by side}
  \centering
  \comparisontable{Bitcoin}{MeshCore}{%
    Curve(s)      & secp256k1               & Ed25519 + X25519 \\
    Identity      & address = hash(pubkey)  & pubkey prefix (1--3\,B) \\
    Signatures    & every transaction       & adverts only \\
    Encryption    & none (all public)       & X25519 (DMs), AES-128 (groups) \\
    The key's job & authorize spending      & protect content / identity \\
    Address reuse & rotated (HD wallets)    & sticky, stay discoverable \\
  }

  \vspace{0.8em}
  {\footnotesize Routing prefix: 1 byte originally (collisions); 1--3 bytes
  since firmware 1.14+.}
\end{frame}

% ---------------------------------------------------------------- channel hash
% A Bitcoiner spots SHA256-twice in the channel hash and reaches for "mining".
% Same primitive, different game — make the distinction, then name the parallel
% that *does* hold (commitment + truncation trade-off).
% Construction VERIFIED against MeshCore source (firmware + michaelhart/
% meshcore-decoder, src/crypto/channel-crypto.ts; "Hitchhiker's Guide to
% MeshCore Cryptography"): for a "#room", key = SHA256("#room")[:16] (the
% AES-128 key), channel id = SHA256(key)[0] (first byte). Cipher is AES-128-ECB
% (+ HMAC-SHA256 MAC) — NOT AES-256/CTR.
\fillerslide[Same primitive, a different game]{Double SHA256 --- but not mining}

\begin{frame}{Joining \texttt{\#bitcoin-at}: SHA256, twice, cropped}
  \texttt{SHA256(SHA256(\#name)[:16])} --- hash the channel name (the \texttt{\#}
  counts), \textbf{crop} to
  16 bytes (the AES-128 key), hash again, take the first byte as a tiny public
  channel id. First pass: a KDF (name\,$\to$\,AES-128 key); second: a commitment
  (a short id for that key). Two SHA256 rounds \emph{look} like Bitcoin --- but
  this is not mining.
  \vfill
  \centering\small
  \setlength{\tabcolsep}{8pt}\renewcommand{\arraystretch}{1.3}%
  \begin{tabular}{@{}lccc@{}}
    \toprule
      & \textcolor{baRed}{\textbf{MeshCore}} & \multicolumn{2}{c}{\textbf{Bitcoin}} \\
    \cmidrule(lr){3-4}
      & channel hash & txid / block id & \textbf{mining} \\
    \midrule
      Construction      & SHA256 + crop + SHA256 & SHA256d (full) & SHA256d (full) \\
      Computed          & once, fixed            & once, fixed    & \textbf{nonce search} \\
      Difficulty target & no                     & no             & \textbf{yes} \\
      Purpose           & id / commitment        & id / commitment & proof-of-work \\
    \bottomrule
  \end{tabular}
  \vfill
  \begin{alertblock}{The parallel that holds}
    No nonce search, no target, no contest --- so not mining. The real Bitcoin
    echo is \textbf{a short one-way commitment plus truncation as a namespace
    trade-off} --- like HASH160 addresses or txid prefixes, shorter is cheaper
    but collides sooner, the same tension as the 1\,B\,$\to$\,1--3\,B prefix.
  \end{alertblock}
\end{frame}

% --------------------------------------------------------------- ASIC / silicon
% Follows the "not mining" slide: a Bitcoiner asks whether LoRa has ASICs too.
% It does -- the LoRa PHY (chirp spread spectrum) is implemented in a dedicated
% radio IC, the Semtech SX1262 (the modem runs in hardware; the MCU just hands it
% bytes over SPI; ~uA in sleep). VERIFIED: Semtech SX1261/SX1262 datasheet/product
% pages; the Heltec LoRa 32 V3 in this deck uses an SX1262; ST's STM32WL packs the
% same SX126x radio IP onto an MCU die. The analogy BREAKS on the reason: a mining
% ASIC wins a competitive PoW race (more efficiency -> more reward, difficulty
% follows); the SX1262 is fixed-function and just saves power -- no contest.
\fillerslide[Same silicon idea, opposite reason]{ASICs --- and the LoRa radio chip}

% Float a small photo of the SX1262 high in the top-right corner of the
% bridgeslide — small enough to sit up by the title/page number, not over the
% MeshCore column. (Bare name; resolves via the deck's pix/ graphics path.)
\cornerimage[width=0.10\paperwidth,topclear=20pt]{sx1262}
\bridgeslide{Purpose-built silicon, for opposite reasons}{%
  \begin{itemize}
    \item Mining \textbf{ASICs} race to compute SHA256 hashes
    \item Efficiency wins \textbf{reward} --- an arms race; more hashpower is
      always better, and difficulty just pulls up to match
  \end{itemize}}{%
  \begin{itemize}
    \item The \textbf{SX1262} radio chip runs chirp spread-spectrum in hardware
      --- the MCU just hands it bytes over SPI
    \item Dedicated silicon for \textbf{power}: $\sim\mu$A asleep, so a coin cell
      lasts; doing the modem on a general CPU would burn far more
  \end{itemize}}{%
  Both are purpose-built chips --- but Bitcoin's ASIC wins a \emph{contest} (more
  efficiency $\to$ more reward), while the SX1262 just \emph{sips power} at a
  fixed job. No race to win: the ceiling is physics and the duty cycle, not rival
  hashpower.}

% ---------------------------------------------------------- brainwallet rooms
% A #hashtag room derives its AES-128 key straight from the room name:
% key = SHA256("#" + name)[:16] (consistent with the channel-hash slide above).
% No salt, no stretching -> a brainwallet. VERIFIED against the MeshCore source
% and jacksbrain.com "Hitchhiker's Guide to MeshCore Cryptography": room names
% are lowercase alphanumerics + hyphens, <=30 chars; every name under 7 chars is
% exhaustively brute-forced in ~90 s on a 2023 laptop GPU (100M+ keys/s). A
% public WebGPU brute-forcer exists: github.com/jkingsman/meshcore-hashtag-cracker
% (added to the references slide). NOT an AES break -- AES-128 is untouched; the
% effective security is the entropy of the *name*. Hashtag rooms are world-
% readable *by design* (the honest divergence from the Bitcoin footgun).
\fillerslide[The name is the key --- a familiar footgun]{Hashtag rooms are brainwallets}

\begin{frame}{A hashtag room is a brainwallet}
  We just saw the room key \emph{is} \texttt{SHA256} of the name --- a Bitcoiner
  has seen exactly this go wrong.
  \vskip0.3em
  \centering
  \comparisontable{Bitcoin brainwallet}{MeshCore \#room}{%
    Key from           & \texttt{SHA256(passphrase)} & \texttt{SHA256(\#name)[:16]} \\
    Input              & low-entropy phrase          & short word (\texttt{\#vienna}) \\
    Salt / stretching  & none                        & none \\
    Public oracle      & the blockchain              & the broadcast packet \\
    Failure            & swept in seconds            & anyone who guesses reads it \\
  }
  \vfill
  \begin{alertblock}{The deep point --- a free verification oracle}
    Both hand the attacker a public way to \emph{check} an offline guess: the
    chain says ``this address holds coins,'' the broadcast says ``this key decodes
    to a message.'' Offline guessing \emph{plus} public verification is what makes
    both catastrophic --- \textbf{not} an AES break: the entropy is the
    \emph{name}. A \#room name under 7 chars falls in $\sim$90\,s on a laptop GPU.
  \end{alertblock}
\end{frame}

\begin{frame}{\dots\ but public \emph{by design}}
  \begin{itemize}
    \item \textbf{Lower stakes:} a brainwallet loses \emph{value}, irreversibly;
      a hashtag room loses \emph{confidentiality} (eavesdropping), not coins.
    \item \textbf{And it flatters MeshCore:} hashtag rooms are labelled
      \textbf{world-readable by design} --- ``no key to remember'' is the feature,
      not a hidden footgun. MeshCore \emph{tells} you the room is public.
    \item \textbf{Know which layer is public:} private traffic gets real crypto
      --- X25519\,/\,Ed25519 DMs, or private channels with long random hex keys.
  \end{itemize}
  \vfill
  \begin{alertblock}{The line}
    You wouldn't put your coins in \texttt{SHA256(``correct horse'')}. Don't put
    your secrets in \texttt{\#vienna}.
  \end{alertblock}
\end{frame}

% ============================ what Bitcoin's consensus buys, and MeshCore skips
% A four-slide section flowing from one insight: Bitcoin defends a scarce asset
% (coins), so it pays for heavy consensus machinery (PoW/Sybil resistance, one
% global order, permanent state); MeshCore defends nothing scarce -- it relays
% fleeting messages -- so it needs none of it. Each slide is one face of that.
\fillerslide[No scarce asset, no heavy machinery]{What Bitcoin's consensus buys --- and MeshCore skips}

% #1 Sybil resistance -- answers the question the "not mining" slide opens.
\fillerslide[Permissionless without proof-of-work]{Sybil resistance}

\bridgeslide{What proof-of-work buys}{%
  \begin{itemize}
    \item PoW makes identity / participation deliberately \textbf{costly}
    \item That cost \emph{is} the security: Sybil resistance, global ordering,
      permanent state
    \item You spend energy to earn a say
  \end{itemize}}{%
  \begin{itemize}
    \item Identity is \textbf{free} --- an Ed25519 keypair, generated offline, in
      unlimited number
    \item No proof-of-work, no cost to participate
    \item Anyone can flood adverts
  \end{itemize}}{%
  No cryptographic Sybil resistance. The only brakes are \textbf{physical}
  (airtime / duty cycle, RF range --- spam self-limits) and \textbf{social}
  (repeater ACLs, admin passwords). MeshCore needs no PoW because it defends no
  scarce asset --- the worst case is wasted airtime, not theft.}

% #2 Incentives -- paid security vs a volunteer commons.
\fillerslide[Who runs the infrastructure --- and why]{Incentives}

\bridgeslide{Paid security vs.\ a volunteer commons}{%
  \begin{itemize}
    \item Miners are \textbf{paid} --- block subsidy + fees
    \item Scarce block space $\to$ a \textbf{fee market} (a price)
    \item Incentives solve the free-rider problem
  \end{itemize}}{%
  \begin{itemize}
    \item Repeater operators are \textbf{unpaid} --- pure goodwill
    \item Scarce airtime $\to$ \textbf{social etiquette}: don't spam Public,
      use \#test, keep advert intervals conservative
    \item No price, no reward
  \end{itemize}}{%
  Bitcoin engineers incentives so the network sustains itself without trust;
  MeshCore leans on volunteers and norms --- a commons with no fee market. It
  works at today's scale, but nothing \emph{economic} defends it against the
  tragedy of the commons.}

% #3 State -- permanent ledger vs fleeting broadcast.
\fillerslide[Permanent ledger vs.\ fleeting broadcast]{State}

\bridgeslide{Consensus state vs.\ the moment}{%
  \begin{itemize}
    \item A permanent, globally agreed ledger
    \item Confirmed once $\to$ there forever, verifiable by anyone, later
  \end{itemize}}{%
  \begin{itemize}
    \item Channel messages exist \textbf{only as they are transmitted}
    \item Offline when it's sent $\to$ you miss it, silently, with no indication
    \item Room servers add \emph{optional} short-term history
  \end{itemize}}{%
  Bitcoin's whole purpose is durable shared state; MeshCore channels deliberately
  keep none. Different goals --- you can't ``re-sync'' a missed channel message
  the way a node replays the chain from genesis. (DMs can ACK, and
  identities/contacts do persist --- the contrast is \emph{channels}, not all of
  MeshCore.)}

% #4 Time -- ordering without a global clock. VERIFIED (see note below); the
% rule is scoped to ADVERTS, not arbitrary packets.
% VERIFIED against the MeshCore firmware behaviour via two sources independent of
% the "Hitchhiker's Guide" blog: DeepWiki's firmware annotation (ripplebiz/
% MeshCore 10.3 Troubleshooting) -- "Timestamp must be greater than
% last_advert_timestamp. If not, advert is rejected as possible replay attack"
% (src/helpers/BaseChatMesh.cpp) -- and the official FAQ (docs/faq.md), which
% documents the clock-not-set failure mode (a node with the wrong time stops
% appearing in discovery). IMPORTANT SCOPE FIX vs the brief: the forward-in-time
% rule is enforced on a node's *adverts* (self-announcements), keyed per pubkey
% by last_advert_timestamp -- NOT on every data packet. Slide reworded to say
% "adverts" so the claim stays watertight.
\fillerslide[Ordering without a global clock]{Time}

\bridgeslide{Two ways to agree on ``after''}{%
  \begin{itemize}
    \item One chain orders \textbf{everything}, globally
    \item That total order is exactly what makes double-spend impossible
  \end{itemize}}{%
  \begin{itemize}
    \item A node's \textbf{adverts} must move \textbf{forward in time}: a repeater
      rejects one whose timestamp is $\le$ the last it saw for that key
      (\texttt{last\_advert\_timestamp}) --- a per-sender anti-replay rule
    \item Reset your radio with the wrong clock $\to$ your adverts are silently
      dropped and you stop appearing on the mesh
  \end{itemize}}{%
  Bitcoin builds one \emph{global} order (essential against double-spend);
  MeshCore enforces only \emph{per-sender} local order on adverts. There's no
  global ``which came first'' across senders --- with no asset to double-spend,
  it doesn't need one.}

% --------------------------------------------------------------- region codes
% The region "transport code" is an HMAC of the region key over the packet
% payload, truncated to 2 bytes; repeaters recompute it per hop to decide
% whether to relay (geofencing the flood). VERIFIED against MeshCore source
% (meshcore-decoder src/crypto/region-transport.ts; DeepWiki 7.6 "Region
% Filtering and Transport Codes"): region key = SHA256(regionName)[:16];
% code = LE-uint16 of the first 2 bytes of HMAC-SHA256(key, payloadType ||
% payload), with 0/0xFFFF nudged to 1/0xFFFE. Computed over the ENCRYPTED
% payload, so repeaters filter by region without decrypting. Symmetric and only
% 2 bytes wide => a routing/airtime filter, not a signature.
\fillerslide[Borders agreed by name, enforced per hop]{The region transport code}

% Plain-language setup before the bridge: the audience doesn't know MeshCore, so
% explain what a region code is and why it exists before drawing the SIGHASH bridge.
\begin{frame}{What is a region code, and why?}
  \begin{columns}[T]
    \begin{column}{0.58\textwidth}
      \begin{itemize}
        \item MeshCore \textbf{floods} adverts and path discovery. Unchecked,
          traffic from far away wastes everyone's scarce airtime --- the same
          budget the duty cycle already rations
        \item A \textbf{region} is just an agreed name --- e.g.\ \texttt{at} for
          Austria, \texttt{at-w} for Vienna. Hash the name and everyone there
          shares a \textbf{region key}
        \item Every packet carries a 2-byte \textbf{transport code} derived from
          that key; a repeater relays a packet only if the code matches its own
          region
        \item The mesh is thus \textbf{geofenced by agreement} --- no registry,
          no coordinates, no permission: just a name the locals share
      \end{itemize}
    \end{column}
    \begin{column}{0.40\textwidth}
      \centering
      \includegraphics[width=\linewidth]{mcaustria-regions}\\[0.3em]
      {\scriptsize Austria carved into shared regions ---
       \texttt{at-west} / \texttt{at-ost} / \texttt{at-sued}\\
       \href{https://meshcore-austria.at}{meshcore-austria.at}
       \;\textbullet\; map © Westermann Gruppe}
    \end{column}
  \end{columns}
  \vfill
  \begin{alertblock}{The neat part}
    The code is computed over the \emph{encrypted} payload, so repeaters filter by
    region \textbf{without decrypting} --- and because it commits to the packet's
    bytes, it behaves like a SIGHASH. That bridge is next.
  \end{alertblock}
\end{frame}

\bridgeslide{The region rides inside the message}{%
  \begin{itemize}
    \item A signature commits to the \textbf{transaction itself} (SIGHASH)
    \item Lift it onto a different tx and it is invalid --- the authorization is
      bound to the content it authorizes
  \end{itemize}}{%
  \begin{itemize}
    \item The 2-byte region code is an \textbf{HMAC over the packet payload};
      repeaters recompute it per hop to decide whether to relay
    \item The region key is just \texttt{SHA256(name)[:16]} --- agree on
      ``\texttt{at-w}'', no key exchange, no registry
  \end{itemize}}{%
  Symmetric and public-ish (anyone who knows the region name can forge it), and
  only 2 bytes wide --- a flood-scoping filter that saves airtime, not a real
  signature. It binds \emph{scope} to the content, not \emph{authorship}.}

% ------------------------------------------------------------------- hard fork
% The firmware 1.14+ multi-byte routing prefix is backward-incompatible: a
% genuine "hard fork" framing — with the honest caveat that there is no chain
% to actually split.
\fillerslide[Break the wire format, split the network]{The multi-byte routing hard fork}

\bridgeslide{A backward-incompatible rule change}{%
  \begin{itemize}
    \item A \textbf{hard fork} changes the rules so old software rejects the new
      format
    \item Old and new nodes can no longer agree --- the network splits unless
      everyone upgrades
  \end{itemize}}{%
  \begin{itemize}
    \item Firmware \textbf{1.14+} widens the routing prefix from 1 byte to
      1--3 bytes
    \item Old firmware cannot parse the new adverts / path hashes --- you must
      upgrade to stay on the mesh
  \end{itemize}}{%
  MeshCore has no consensus and no ledger to fork: this is a coordinated
  firmware upgrade, not a persistent chain split. ``Hard fork'' names the
  backward incompatibility --- not a permanent divergence you could mine on.}

% ------------------------------------------------------------------ references
% Collected links — project/docs, the live observer, and the internals/crypto
% sources behind the channel-hash, region-code and AES-128 facts in this deck.
\begin{frame}{References \& further reading}
  \footnotesize
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \textbf{Project, docs \& live}
      \begin{itemize}
        \item \href{https://meshcore.io}{meshcore.io} --- project home
        \item \href{https://docs.meshcore.io}{docs.meshcore.io} --- user \&
          protocol docs
        \item \href{https://github.com/meshcore-dev/MeshCore}{github.com/meshcore-dev/MeshCore}
          --- firmware source
        \item \href{https://logger-at.meshcore.observer}{logger-at.meshcore.observer}
          --- the Austrian packet observer
      \end{itemize}
      \medskip
      \textbf{Hardware \& radio}
      \begin{itemize}
        \item \href{https://www.semtech.com/products/wireless-rf/lora-connect/sx1262}{Semtech SX1262}
          --- the LoRa radio IC (chirp spread spectrum in silicon)
        \item \href{https://www.st.com/en/microcontrollers-microprocessors/stm32wl5x-ex.html}{ST STM32WL}
          --- the same SX126x radio IP on an MCU die
      \end{itemize}
    \end{column}
    \begin{column}{0.5\textwidth}
      \textbf{Internals \& crypto}
      \begin{itemize}
        \item \href{https://deepwiki.com/meshcore-dev/MeshCore}{deepwiki.com/meshcore-dev/MeshCore}
          --- annotated internals (incl.\ region filtering \& transport codes)
        \item \href{https://jacksbrain.com/2026/01/a-hitchhiker-s-guide-to-meshcore-cryptography/}{A
          Hitchhiker's Guide to MeshCore Cryptography} --- jacksbrain.com
        \item \href{https://github.com/michaelhart/meshcore-decoder}{michaelhart/meshcore-decoder}
          --- crypto-complete packet decoder
        \item \href{https://www.localmesh.nl/en/meshcore-encryption-details/}{localmesh.nl}
          --- encryption details (AES-128, not AES-256)
        \item \href{https://github.com/jkingsman/meshcore-hashtag-cracker}{jkingsman/meshcore-hashtag-cracker}
          --- WebGPU brute-forcer for \#hashtag-room keys
        \item \href{https://docs.meshcore.io/packet_format/}{docs.meshcore.io/packet\_format}
          --- the on-wire packet format
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

% Closing call to action: concrete Bitcoin channels the audience can join right
% now. Ties back to the region-transport-code slide (a region is just an agreed
% name: "at" = Austria, "at-w" = Vienna; no region = mesh-wide).
\begin{frame}{Join the Bitcoin channels on MeshCore}
  Same name, same key --- just type the channel name to join.
  \vskip1.2em
  \begin{itemize}
    \item \texttt{\#bitcoin} --- no region (mesh-wide)
    \item \texttt{\#bitcoin-at} --- region \texttt{at} (Austria)
    \item \texttt{\#bitcoin-wien} --- region \texttt{at-w} (Vienna)
  \end{itemize}
  \vfill
  \begin{alertblock}{Pick your scope}
    Plain \texttt{\#bitcoin} reaches the whole mesh; the regional channels add a
    \textbf{region code} so repeaters relay them only locally --- less airtime,
    same permissionless join.
  \end{alertblock}
\end{frame}

\fillerslide[%
  \href{https://meshcore.io}{meshcore.io}\quad\textbullet\quad
  \href{https://logger-at.meshcore.observer}{logger-at.meshcore.observer}%
]{Thank you}

% ============================================================== backup / appendix
% Optional deep-dive on the on-wire packet format. Kept AFTER the closing as a
% backup slide so it can be skipped for a non-technical audience and shown only
% on demand — it's self-contained, so skipping it breaks nothing in the main
% talk. It also ties several earlier slides together (region code, the 1--3 byte
% routing prefix, AES-128 + 2-byte MAC). [noframenumbering] keeps it out of the
% main slide count. VERIFIED against docs.meshcore.io/packet_format, DeepWiki 7.1
% "Packet Structure and Types", and michaelhart/meshcore-decoder.
\begin{frame}[noframenumbering]{Backup --- anatomy of a MeshCore packet}
  \small
  \texttt{[\,header\,][\,transport codes?\,][\,path\_len\,][\,path\,][\,payload\,]}
  \hfill at most \textbf{255\,bytes} --- one LoRa frame
  \medskip
  \begin{itemize}
    \item \textbf{header} (1\,B) --- 2 bits route type, 4 bits payload type
      (advert, txt, group-text, ack, \dots), 2 bits version
    \item \textbf{transport codes} (4\,B, optional) --- only on \emph{transport}
      routes; where the 2-byte \textbf{region code} rides
    \item \textbf{path} (length byte $+$ up to 64\,B) --- \textbf{the core idea:}
      each relay \textbf{appends its own short ID} (1--3\,bytes) as it forwards
    \item \textbf{payload} (up to 184\,B) --- type-specific: an \emph{advert} is
      Ed25519-signed; a \emph{group / text} message is channel-hash $+$ 2-byte
      MAC $+$ AES-128 ciphertext
  \end{itemize}
  \vfill
  \begin{alertblock}{This is what makes it routing, not flooding}
    On a flood the path \emph{records} the relays a packet crossed; a direct
    message then \emph{carries that exact list} of router IDs, so only those
    repeaters forward it --- not every node, the way Meshtastic floods. The path
    \emph{is} the route.
  \end{alertblock}
  {\footnotesize Skip unless the room likes wire formats. Sources:
  \texttt{docs.meshcore.io/packet\_format}, DeepWiki 7.1, \texttt{meshcore-decoder}.}
\end{frame}

\end{document}
