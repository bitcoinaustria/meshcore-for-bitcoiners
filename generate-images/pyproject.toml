[project]
name = "generate-images"
version = "0.1.0"
description = "AI image generation (fal.ai + Replicate) for the MeshCore for Bitcoiners deck"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fal-client>=0.7.0",
    "httpx>=0.28.1",
    "replicate>=1.0.7",
]
